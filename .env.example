# Token para abrir /admin y descargar el CSV. Si lo dejas vacío, el panel queda abierto.
ADMIN_TOKEN=cambia-esto

# La crea Vercel sola al conectar una base Postgres (Storage > Postgres/Neon).
POSTGRES_URL=
